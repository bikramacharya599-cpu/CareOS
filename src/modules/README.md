Module boundaries are reserved for incremental extraction from the existing UI.

Current route components remain in `main.jsx` to preserve the working prototype while service, model, permission, and audit boundaries are introduced.
