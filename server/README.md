# CareOS API

Express REST API foundation for local development. It uses fictional/demo-safe configuration only and does not implement authentication or data migration.

Run from the repository root with `npm run server:dev` or `npm run server:start`.